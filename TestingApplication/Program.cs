using OpenQA.Selenium;
using OpenQA.Selenium.Firefox;
using System.Threading;
namespace TestingApplication
{
    class EnteryPoint
    {
        static void Main()
        {
         //   ClassName check = new ClassName();
           // MessageColor mColor = new MessageColor();
           // InputText textboxcheck = new InputText();
            CheckBox test = new CheckBox();
            test.raioButton();
           // check.FoundByClass();
           // Thread.Sleep(5000);
           // check.ByNameSelector();
           // textboxcheck.checkInbox();

        }
    }
}
